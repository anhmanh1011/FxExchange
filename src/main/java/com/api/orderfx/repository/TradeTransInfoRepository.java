package com.api.orderfx.repository;

import com.api.orderfx.entity.TradeTransInfoEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TradeTransInfoRepository extends JpaRepository<TradeTransInfoEntity, Long> {
}
